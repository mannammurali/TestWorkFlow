[sqlfluff]
dialect = tsql

rules = L003, L006, L009  # Keep only indent and comma-related fixes

# Disable specific rules causing issues
exclude_rules = CP02, LT05  # Turn off capitalization and long line checks

max_line_length = 120  # Allow lines up to 120 characters
